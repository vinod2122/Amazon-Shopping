eureka:
  client:
    registerWithEureka: true
    fetchRegistry: true
    service-Url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    hostname: localhost
server:    
  port: 9999
spring:
  application:
    name : AMAZON-SHOPPING